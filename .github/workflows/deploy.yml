name: Deploy

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - staging
          - production

env:
  NODE_VERSION: '24'

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    environment: >-
      ${{
        (github.ref == 'refs/heads/main' && 'production') ||
        (github.ref == 'refs/heads/develop' && 'staging') ||
        github.event.inputs.environment
      }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint


      - name: Run tests
        run: npm run test:run
        env:
          NODE_ENV: test

      - name: Build
        run: npm run build
        env:
          NODE_ENV: production

      - name: Setup SSH
        uses: ./.github/actions/setup-ssh # Or use swb-infra shared action
        with:
          host: ${{ vars.SSH_HOST }}
          private-key: ${{ secrets.SSH_KEY }}
          username: ${{ vars.SSH_USER }}
          port: ${{ vars.SSH_PORT }}

      - name: Validate deployment variables
        run: |
          if [ -z "${{ vars.HTML_DIR }}" ]; then
            echo "Error: HTML_DIR GitHub variable is not set"
            exit 1
          fi
          echo "HTML_DIR: ${{ vars.HTML_DIR }}"

      - name: Deploy to remote
        env:
          HTML_DIR: ${{ vars.HTML_DIR }}
        run: |
          # Create target directory for static files
          ssh server <<EOF
            mkdir -p $HTML_DIR
          EOF

          # Copy built files to server and remove stale assets
          rsync -avz --delete -e 'ssh' dist/ server:$HTML_DIR
